<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Create H1 with JS</title>
</head>
<body>

<!-- 
  Question:
  Create an <h1> element dynamically using JavaScript.
  Apply the following CSS properties via JavaScript:
  - Color: blue
  - Font size: 22px
  Also, display the same text styled in the console.
-->

<script>
  // 1. Create the <h1> element
  const h1 = document.createElement("h1");

  // 2. Set its text content
  h1.textContent = "alzero";

  // 3. Apply CSS via JavaScript
  h1.style.color = "blue";
  h1.style.fontSize = "22px";

  // 4. Append the <h1> to the body
  document.body.appendChild(h1);

  // 5. Log the same text in console with styling
  console.log("%calzero", "color: blue; font-size: 22px");
</script>

</body>
</html>
